// Barral file = Arquivo que exporta todos os arquivos que tem aqui dentro

import { questions } from "./questions.ts";
import { rooms } from "./rooms.ts";

export const schema = {
    rooms,
    questions
}